{
  "name": "heimdall",
  "type": "module",
  "version": "1.0.0",
  "description": "Express API for ACM User Authentication",
  "main": "src/index.js",
  "scripts": {
    "start": "node --experimental-specifier-resolution=node src/index.js",
    "build": "rm -rf build && mkdir build && babel -d ./build ./src -s",
    "dev": "nodemon --exec babel-node --experimental-specifier-resolution=node src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Hassam Uddin",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "jsonwebtoken": "^8.5.1",
    "nodemailer": "^6.4.11"
  },
  "devDependencies": {
    "@babel/cli": "^7.15.7",
    "@babel/core": "^7.15.8",
    "@babel/node": "^7.10.5",
    "@babel/preset-env": "^7.15.8",
    "nodemon": "^2.0.4"
  }
}